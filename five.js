let a = +prompt('a sonni kiriting'),
    b = +prompt('b sonni kiriting'),
    c = +prompt('c sonni kiriting'),
    d = +prompt('d sonni kiriting');

if(a > 0 && b > 0 && c > 0 && d > 0){
console.log('musbat');
}else{
    console.log('manfiy');
}